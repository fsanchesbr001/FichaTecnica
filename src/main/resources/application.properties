spring.profiles.active=${APP_PROFILE:development}
spring.application.name=fichaTecnica

springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha

spring.datasource.url=jdbc:mysql://${DB_IP:localhost}:${DB_PORT:3306}/${DB_NAME:FichaTecnica}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PWD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


api.security.token.secret=${JWT_SECRET:F@br1c10S@nch3s}
api.security.token.expiration-minutes=${JWT_EXPIRATION_MINUTES:120}
api.security.token.time-zone=${JWT_TIME_ZONE:America/Sao_Paulo}

spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.connection-timeout=20000

spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.auto-commit=false
spring.datasource.hikari.transaction-isolation=TRANSACTION_READ_COMMITTED

# Caminho base para armazenamento no droplet DigitalOcean
digitalocean.storage.base-path=${DO_STORAGE_PATH:/olivander/ficha_tecnica/imagens}

# URL p?blica base para acesso ?s imagens (servida via Nginx em produ??o)
# Exemplo producao: https://meudominio.com/uploads
digitalocean.storage.public-url=${DO_STORAGE_PUBLIC_URL:http://localhost:8080/uploads}

# Tamanho m?ximo do arquivo (50MB)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Serve imagens localmente em dev (mapeada para digitalocean.storage.base-path)
spring.web.resources.static-locations=classpath:/static/,file:${DO_STORAGE_PATH:/olivander/ficha_tecnica/imagens}/
spring.mvc.static-path-pattern=/uploads/**

# Infos de servidor de email
spring.mail.host=${MAIL_HOST:smtp.zoho.com}
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=${MAIL_USER:contato@fabriciosanches.com}
spring.mail.password=${MAIL_PWD:k7bk9tqCGBYy}
spring.mail.protocol=smtps
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.zoho.com
spring.mail.test-connection=false
spring.mail.properties.mail.smtp.from=contato@fabriciosanches.com

ficha-tecnica.mail.from=contato@fabriciosanches.com

ficha-tecnica.system.username=${SYSTEM_USER:contato@fabriciosanches.com}
ficha-tecnica.system.password=${SYSTEM_PWD:@n@M@r1@S@nch35!1989}
